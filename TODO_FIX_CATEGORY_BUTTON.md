# AUFGABENLISTE: FIX STARTBILDSCHIRM KATEGORIE-BUTTON

## Ziel
Behebung des Fehlers, bei dem das Kategorie-Menü am Startbildschirm nur kurz aufflackert und nur bei gedrückt halten geöffnet bleibt.

## Status-Tracking
- [ ] **Teilschritt 1: Initialisierung & Dokumentation**
    - [x] Repository geklont und `manus.read` analysiert.
    - [x] Aufgabenliste erstellt und gepusht.
- [ ] **Teilschritt 2: Fehleranalyse**
    - [ ] Analyse von `index.html`, `style.css` und `app.js` bezüglich des Kategorie-Modals/Buttons.
    - [ ] Identifikation des fehlerhaften Event-Listeners oder CSS-States (z.B. `pointerdown` vs `click` Konflikt oder `:active` State).
- [ ] **Teilschritt 3: Fix-Implementierung**
    - [ ] Korrektur der Event-Logik in `app.js`.
    - [ ] Anpassung der CSS-Transitions/Displays falls erforderlich.
- [ ] **Teilschritt 4: Validierung**
    - [ ] Lokaler Build-Test mit `pnpm build`.
    - [ ] (Optional) Manueller Test-Script Check.
- [ ] **Teilschritt 5: Abschluss & Push**
    - [ ] Dokumentation in `manus.read` (AUSGEFÜHRTE SCHRITTE).
    - [ ] Finaler Push aller Änderungen.

---
*Diese Liste wird nach jedem Teilschritt aktualisiert und gepusht.*
